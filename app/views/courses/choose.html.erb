<h2> Wähle nun deine Lieblingskurse:</h2>

<p class="lead">
  Hier siehst du alle Kurse die für dich in Frage kommen. Weiter unten kannst du deine Lieblingskurse auswählen
</p>

<h3> Die möglichen Kurse:</h3>
<table class="table table-striped table-bordered">
  <tr>
    <th>Titel</th>
    <th>Beschreibung</th>
    <th>findet wo statt</th>
  </tr>

  <% @courses.each do |course| %>
      <tr>
        <td><%= course.title %></td>
        <td><%= course.description %></td>
        <td><%= course.where %></td>
      </tr>
  <% end %>
</table>

<h3> Deine Wahl:</h3>

<%= form_tag mychoose_courses_path, class: "form-horizontal" do %>
    <div class="control_group">  
        <%= label_tag(:first, "Erste Wahl:", class: "control-label") %>
      <div class="controls">
        <%= select_tag :first, options_from_collection_for_select(@courses, :id, :title, @courses[0].id) %>
      </div>
    </div>
    <div class="control_group">  
        <%= label_tag(:second, "zweite Wahl:", class: "control-label") %>
      <div class="controls">
        <%= select_tag :second, options_from_collection_for_select(@courses, :id, :title, @courses[1].id) %>
      </div>
    </div>
    <% if @courses.length > 2 %>
        <div class="control_group">  
            <%= label_tag(:third, "dritte Wahl:", class: "control-label") %>
          <div class="controls">
            <%= select_tag :third, options_from_collection_for_select(@courses, :id, :title, @courses[2].id) %>
          </div>
        </div>
    <% end %>
    <div class="form-actions">
      <%= submit_tag("Wähle", class: "btn btn-primary") %>
    </div>
    </div>
<% end %>