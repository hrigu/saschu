<p>
  Du hast deine Auswahl getroffen. Unten siehst du deine Lieblingskurse:
</p>

<table class="table table-striped table-bordered">
  <tr>
    <th>Priority</th>
    <th>Title</th>
    <th>Description</th>
    <th>Where</th>
  </tr>

  <% @choosen_courses.each do |cc| %>
      <tr>
        <td><%= cc.priority %></td>
        <td><%= cc.course.title %></td>
        <td><%= cc.course.description %></td>
        <td><%= cc.course.where %></td>
      </tr>
  <% end %>
</table>
